export class LifePasses{
  date: string;
  title: string;
  body: string;

  constructor(date: string, title: string, body: string) {
    this.date = date;
    this.title = title;
    this.body = body;
  }
}
